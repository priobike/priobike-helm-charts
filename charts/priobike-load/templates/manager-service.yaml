apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.manager.serviceName }}
spec:
  selector:
    app: {{ .Values.manager.labels.app }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
